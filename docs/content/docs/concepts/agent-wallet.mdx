---
title: Agent Wallet
description: Privy-managed agent wallets on Base
---

ZenithPay's agent wallet is built using Privy managed wallets.

Each agent gets its own wallet address on Base, with private keys managed server-side by Privy (the ZenithPay API never handles raw keys).

## Privy Managed Wallets

ZenithPay uses Privy to create a real wallet for each AI agent on Base.

The important security property is simple:
private keys are managed by Privy's infrastructure. The ZenithPay API never handles raw wallet private keys.

### Onboarding: `POST /wallet/genesis`
REST onboarding delegates wallet creation to Privy via the agent environment.

Example:
```bash
curl -X POST https://zp-api-q4hd.onrender.com/wallet/genesis \
  -H "Authorization: Bearer $ZENITHPAY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "agent@yourdomain.com",
    "label": "research-agent-01"
  }'
```

The response includes the agent wallet address and initial balances.

## Owner-agent linking model

ZenithPay links each agent wallet to an owner EOA during onboarding/policy activation.

- owner account controls policy and approvals
- agent wallet executes autonomous payments within those limits
- dashboard data resolves by connected owner, then linked agents

## Agent tools (MCP + Skill)

Wallet and payment operations are exposed to agents through MCP tools:

- `zenithpay_balance`
- `zenithpay_get_limits`
- `zenithpay_verify_merchant`
- `zenithpay_pay_service`
- `zenithpay_ledger`
